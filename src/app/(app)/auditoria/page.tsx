import { requireCompany } from "@/lib/auth"; import { prisma } from "@/lib/prisma";
export default async function Page(){const {user,companyId}=await requireCompany();if(user.role!=="OWNER") return <div className="alert alert-error">Acesso exclusivo do owner.</div>;const logs=await prisma.auditLog.findMany({where:{companyId},include:{user:true},orderBy:{createdAt:"desc"},take:200});return <div><div className="page-header"><div><span className="eyebrow">Segurança</span><h1>Auditoria</h1><p>Rastreabilidade das ações críticas da clínica.</p></div></div><section className="card section-card"><div className="table-wrap"><table><thead><tr><th>Data</th><th>Usuário</th><th>Ação</th><th>Entidade</th><th>Descrição</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{l.createdAt.toLocaleString("pt-BR")}</td><td>{l.user?.name??"Sistema"}</td><td>{l.action}</td><td>{l.entityType}</td><td>{l.description??"—"}</td></tr>)}{logs.length===0&&<tr><td colSpan={5}>Nenhum evento registrado.</td></tr>}</tbody></table></div></section></div>}
