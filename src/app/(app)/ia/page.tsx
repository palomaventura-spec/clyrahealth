import { prisma } from "@/lib/prisma";import { requireCompany } from "@/lib/auth";import { companyHasFeature } from "@/modules/billing/services/features";import { simulateReceptionAIAction } from "@/modules/ai/actions";export default async function Page(){const {companyId}=await requireCompany();const enabled=await companyHasFeature(companyId,"AI_RECEPTION");const c=await prisma.conversation.findFirst({where:{companyId,phone:"SIMULADOR"},orderBy:{updatedAt:"desc"},include:{messages:{orderBy:{createdAt:"asc"},take:30}}});return <div><div className="page-header"><div><span className="eyebrow">Inteligência Artificial</span><h1>Recepção IA</h1><p>Assistente administrativo de agenda. Não fornece orientação clínica.</p></div></div>{!enabled?<section className="card section-card"><div className="empty-state">Disponível no plano Pro AI / Premium.</div></section>:<div className="two-columns"><section className="card section-card"><h2>Simulador</h2><div className="ai-chat">{c?.messages.map(m=><div key={m.id} className={`ai-message ${m.author==="AI"?"ai-message-bot":"ai-message-user"}`}><small>{m.author}</small><p>{m.content}</p></div>)}</div><form action={simulateReceptionAIAction} className="ai-input"><input name="message" placeholder="Ex.: Quero marcar uma consulta" required/><button className="btn btn-primary">Enviar</button></form></section><section className="card section-card"><h2>Escopo</h2><p>Agendar, confirmar, cancelar, reagendar e consultar horários. Diagnóstico e prescrição ficam fora da IA.</p></section></div>}</div>}
