import { resetPasswordAction } from "@/app/actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <main className="auth-page"><section className="auth-card"><h1>Nova senha</h1>{q.erro&&<div className="alert alert-error">Use uma senha com no mínimo 8 caracteres.</div>}<form action={resetPasswordAction} className="auth-form"><input type="hidden" name="token" value={q.token??""}/><label>Nova senha<input name="password" type="password" minLength={8} required/></label><button className="btn btn-primary">Alterar senha</button></form></section></main>}
