import Link from "next/link";
import { requestPasswordResetAction } from "@/app/actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <main className="auth-page"><section className="auth-card"><Link href="/" className="brand">Clyra<span>Health</span></Link><h1>Recuperar senha</h1><p>Informe o e-mail da sua conta.</p>{q.enviado&&<div className="alert alert-success">Se o e-mail existir, as instruções foram geradas. Em desenvolvimento, veja o terminal.</div>}<form action={requestPasswordResetAction} className="auth-form"><label>E-mail<input name="email" type="email" required/></label><button className="btn btn-primary">Continuar</button></form><Link href="/login">Voltar ao login</Link></section></main>}
