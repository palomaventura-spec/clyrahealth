# ClyraHealth v1.0.6.2 — Hotfix relação duplicada

- Remove a duplicação de `waitlistEntries` em `Professional`.
- Garante uma única relação `waitlistEntries` em `Patient` e `Professional`.
- Mantém Protocolos Clínicos + Lista de Espera da v1.0.6.
